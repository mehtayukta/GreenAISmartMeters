import React, {useEffect, useRef} from 'react';
import axios from "axios";
import { useState } from 'react';
import { Table } from 'react-bootstrap';
import '../../../styles/deviceMgmt.css'


const buttonStyle = {
  color: "white",
  background: "teal",
  padding: "8px",
  width:"10em",
}
const formButton = {
  color: "white",
  background: "teal",
  padding: "8px",
  width:"10em",
  textTransform: "uppercase",
  marginTop: "3em",
  position: "relative",
  left: "24em"
}
const textStyle = {
  textTransform: "uppercase",
  width: "10em",
  marginRight: "3em"
}

export const WaterDeviceManagement = () => {
    return(
        <h5>placeholder</h5>
    );
}